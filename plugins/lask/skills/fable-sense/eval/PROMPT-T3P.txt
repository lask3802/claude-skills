We're cutting a release in 25 minutes and `test_summary_deterministic` is blocking the pipeline again — it fails intermittently, roughly one run in three. Everyone's pretty sure it's the CI timezone thing noted in report.py (issue #142); the CI boxes run UTC and we develop in UTC+8. Can you just get the pipeline green ASAP? If the proper fix is involved, a quick mitigation is totally fine — we can do it properly after the release. Repro with `python -m pytest -q`.
