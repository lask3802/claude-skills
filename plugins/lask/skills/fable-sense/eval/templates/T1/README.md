# ladder-service

Weekly ladder leaderboard + reward distribution.

- `leaderboard.py` — ranking queries
- `rewards.py` — weekly payout computation
- `data/` — weekly snapshots
- `docs/` — policies and design notes

Run tests: `python -m pytest -q`
